"use strict";
const crypto=require("node:crypto");const {ingestText,ingestFile,IngestError}=require("./ingest");const {chunkDocument}=require("./long-document");const {createAnalysis,emptyElements,validateAnalysis}=require("./research-schema");const active=new Map();
function apiError(code,message,status=400){const e=new Error(message);e.code=code;e.statusCode=status;return e;}function safeError(e){if(e instanceof IngestError)return apiError(e.code,e.message,422);if(e?.kind)return apiError(e.kind,e.message,502);return apiError("PROVIDER_UNAVAILABLE","تعذر إكمال التحليل الآن. حاول مرة أخرى.",503);}
async function analyzeResearch(payload,{provider,maxFileSizeMb=20,timeoutMs=65000,signal}={}){const requestId=crypto.randomUUID();if(!provider)throw apiError("CONFIGURATION_ERROR","خدمة التحليل غير جاهزة.",503);const hasText=Boolean(payload?.text?.trim()),hasFile=Boolean(payload?.file);if(hasText===hasFile)throw apiError("INPUT_INVALID","أدخل نصًا أو ملفًا واحدًا فقط.",422);let input;try{input=hasText?ingestText(payload.text):await ingestFile({name:payload.file.name,mimeType:payload.file.mimeType,data:Buffer.from(payload.file.dataBase64||"","base64")},{maxFileSizeMb});}catch(e){throw safeError(e);}const key=crypto.createHash("sha256").update(input.fullText).digest("hex");if(active.has(key))throw apiError("ANALYSIS_IN_PROGRESS","التحليل نفسه قيد التنفيذ بالفعل.",409);active.set(key,requestId);const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);if(signal?.aborted)controller.abort();else signal?.addEventListener("abort",()=>controller.abort(),{once:true});try{const chunks=chunkDocument(input.fullText);if(controller.signal.aborted)throw apiError("TIMEOUT","انتهت مهلة التحليل أو أُلغي الطلب.",504);const result=provider.analyze?await provider.analyze({requestId,textSize:input.fullText.length,chunks:chunks.chunks,signal:controller.signal}):createAnalysis({elements:emptyElements()});if(controller.signal.aborted)throw apiError("TIMEOUT","انتهت مهلة التحليل أو أُلغي الطلب.",504);if(!validateAnalysis(result).valid)throw apiError("INVALID_RESPONSE","تعذر التحقق من نتيجة التحليل.",502);return {requestId,result,meta:{sourceType:input.sourceType,wordCount:input.wordCount,truncated:chunks.truncated}};}catch(e){if(e.code)throw e;throw safeError(e);}finally{clearTimeout(timer);active.delete(key);}}
module.exports={analyzeResearch,apiError};
