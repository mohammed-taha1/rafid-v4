# سجل التغييرات

## 4.3.0 — Shared Accounts, Persistent Workspaces, Server Key

- أصبح مفتاح Groq سرًا خادميًا واحدًا في وضع النشر المشترك؛ لا يصل إلى HTML أو JavaScript أو استجابة API، ولا يستطيع المستخدم تغييره.
- أضيف تسجيل الدخول عبر Google وMicrosoft وGitHub ورابط البريد دون كلمة مرور باستخدام Supabase Auth.
- أضيف حفظ الجلسة وتجديد رمزها تلقائيًا، مع استعادة الحساب بعد إغلاق المتصفح ما لم يسجل المستخدم الخروج أو تُلغَ الجلسة أو تُمسح بيانات المتصفح.
- أضيف جدول `rafid_workspaces` مع Row Level Security بحيث يقرأ كل مستخدم صفه ويعدله وحده.
- أضيف حفظ تلقائي لمساحة العمل المنظمة واستعادتها بعد الدخول؛ لا تُحفظ النصوص الخام أو مفاتيح API.
- يتوقف الحفظ السحابي افتراضيًا للمحتوى السري، ويُمنع تمامًا للمحتوى المقيّد.
- يتحقق خادم رافد من جلسة Supabase قبل الصحة وجلب المصادر وكل طلب AI.
- أضيف حد طلبات مستقل لكل حساب وحد يومي عام لحماية مفتاح وحصة المزود المشتركة.
- أضيفت نقطة إعداد عامة آمنة، ونقطة جلب المصادر إلى نسخة Azure Functions، وتصحيح CORS للسماح برمز Bearer في النشر المنفصل.
- أضيفت حزمة Supabase محلية بلا CDN، واختبارات للمصادقة، واستمرار الجلسة، ومنع الأسرار، وسياسات الحفظ، وحدود الاستخدام.
- أضيف `Dockerfile` لتشغيل الواجهة والخادم على أصل HTTPS واحد في منصة استضافة تدعم الحاويات.

## 4.2.0 — Free Private Groq Path

- أضيف Groq كمسار افتراضي مجاني دون الحاجة إلى Ollama.
- أضيف `openai/gpt-oss-120b` مع Chat Completions وJSON Schema الصارم وreasoning مخفي.
- أضيف دعم Zero Data Retention الخاص بـGroq، مع فصل بيانات الاستخدام الوصفية عن محتوى الطلب.
- يتحقق الخادم من صلاحية مفتاح Groq ووجود النموذج عند التفعيل دون إرسال ملف أو نص مشروع.
- يبقى المفتاح في ذاكرة عملية Node للجلسة فقط، ولا يعود في الاستجابة ولا يُحفظ في المتصفح أو القرص.
- ثُبّت عنوان Groq على النطاق الرسمي لمنع تحويل المفتاح إلى خادم آخر.
- أضيف ضغط حقول فارغة وحدود طلبات محافظة تناسب قيود الخطة المجانية.
- أضيفت اختبارات عقد SDK مستقلة لمسارات OpenAI وOllama وGroq.

## 4.1.0 — Automatic Local Pipeline

- أضيف تشغيل موحد للواجهة وAPI بأمر `npm.cmd run rafid` دون Azure Functions Core Tools.
- أضيف إعداد OpenAI من localhost مع مفتاح محفوظ في ذاكرة العملية للجلسة فقط.
- أضيف Ollama و`gpt-oss:20b` عبر API محلي كمسار لا يرسل محتوى التحليل إلى السحابة.
- أضيف جلب آمن لصفحات الفرص العامة عبر HTTPS مع منع SSRF إلى الشبكات الخاصة.
- أصبح إدخال رابط الفرصة كافيًا لجلب النص واستخراج الشروط تلقائيًا عندما تسمح الصفحة.
- أصبح زر المشروع ينفذ الاستخراج والمطابقة وفتح ملف القرار في مسار واحد.
- أضيف فصل صريح بين `store:false` وZero Data Retention.
- أضيف وضع `strict_zdr` الافتراضي، وحظر المحتوى السري دون ZDR أو موافقة مؤسسية.
- أضيفت سياسة البيانات الفعلية إلى صحة الخادم وبيانات كل استجابة.
- رُفع النموذج الافتراضي إلى `gpt-5.6` مع reasoning effort مرتفع قابل للضبط.

## 4.0.0 — Opportunity Readiness Pilot

- غُيّر محور المنتج من تقييم عام/مطابقة ممولين إلى أهلية مشروع لفرصة محددة.
- أضيف استخراج فرصة التمويل بعقد Structured Outputs واقتباسات مصدرية.
- أضيف محرك مطابقة فرصة/مشروع مع بوابات صارمة وفجوات وخطة عمل وحزمة تقديم.
- أضيف اشتقاق حتمي للأهلية يمنع الدرجة من تجاوز شرط فاشل.
- أضيف إرجاع أي شرط إلزامي أغفله النموذج بحالة «غير معروف».
- أضيف تدفق مراجعة مؤسسية وقياس نتائج التجربة.
- أضيف حاجز خصوصية محلي وخادمي بأربعة تصنيفات ومنع للمحتوى المقيّد.
- أزيلت البيانات الحساسة من السجلات ومخرج الصحة.
- حُدّثت قراءة PDF وDOCX إلى أصول محلية حديثة، مع تعطيل تقييم JavaScript داخل PDF.
- أضيفت بيانات عرض مصطنعة ووثائق قرار وتجربة وخصوصية.
- بقيت V3.1 كمرجع تاريخي داخل `frontend/`.
