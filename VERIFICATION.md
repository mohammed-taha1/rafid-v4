# تقرير التحقق — رافد V4.3 بالحسابات والمفتاح الخادمي

تاريخ البناء: 18 يوليو 2026

## فحوص آلية نجحت

- `npm run check`: فحص صياغة ملفات JavaScript التنفيذية والاختبارات.
- `npm test`: اختبارات الخلفية، عقود OpenAI/Ollama/Groq، المصادقة، منطق الواجهة، التدقيق الثابت، والخادم الموحد.
- تحقق جلسة Supabase محاكى: جلسة مفقودة، جلسة باطلة، جلسة صحيحة، وقراءة هوية المستخدم.
- تحقق أن الإعداد العام يعرض Project URL وPublishable key فقط، ولا يعرض Groq key أو `service_role`.
- تحقق أن Google وMicrosoft وGitHub والبريد تظهر بحسب إعداد الخادم.
- تحقق أن الواجهة تفعل `persistSession` و`autoRefreshToken` وقراءة جلسة العودة.
- تحقق أن طلبات API المحمية ترسل Bearer token، وأن CORS يسمح بترويسة `Authorization` عند فصل الواجهة عن الخادم.
- تحقق أن مساحة العمل تستخدم `rafid_workspaces` وأن الحفظ منفصل بمعرف المستخدم.
- تحقق أن الحفظ السحابي يتوقف للمحتوى السري افتراضيًا ويُمنع للمحتوى المقيّد.
- تحقق أن مفتاح المزود لا يوجد في الواجهة، وأن مكتبة Supabase وأصول PDF/DOCX محلية بلا CDN وقت التشغيل.
- تحقق حد الطلبات لكل مستخدم والحد اليومي العام.
- تحقق منع SSRF إلى الشبكات الخاصة، ومنع المحتوى المقيّد، وتنقيح البريد والهاتف والهوية والمفاتيح.
- تحقق أن مشروعًا بدرجة 99 يبقى غير مؤهل عند فشل بوابة صارمة.
- تحقق عقود Structured Outputs لمسارات OpenAI وOllama وGroq بخوادم محاكاة محلية.
- تحقق تشغيل الواجهة وAPI على أصل واحد عبر `npm start`.
- `npm audit --omit=dev`: لا ثغرات إنتاج معروفة وقت البناء.
- تثبيت نظيف من الحزمة بـ`npm ci` ثم إعادة الفحوص: موثق في خطوة التغليف النهائية.

## ما لم يُنفذ دون حسابات المالك

- لم يُرسل استدعاء حي إلى Groq أو أي مزود نموذج؛ يتطلب مفتاح المالك، ولا يجوز تضمينه في الحزمة أو إرساله للمطور.
- لم يُنفذ OAuth حي مع Google/Microsoft/GitHub أو Magic Link حي؛ يتطلب مشروع Supabase ومفاتيح OAuth وروابط عودة يملكها صاحب المنصة.
- لم يُكتب صف حي إلى Supabase؛ اختُبر منطق الحفظ وسياسات SQL ثابتًا، ويبقى اختبار RLS الحي بعد تشغيل `supabase/rafid_schema.sql` في مشروع المالك.
- اختبار المتصفح المرئي يعتمد على Chromium. التدقيق الثابت واختبارات منطق الواجهة يعملان دون متصفح خارجي.

## حدود يجب ألا تختفي عند الإطلاق

- بقاء الجلسة يعني الاستعادة والتجديد التلقائي، وليس دخولًا أبديًا: الخروج، إلغاء الجلسة، تغيير سياسات Supabase، أو مسح بيانات المتصفح ينهيها.
- حصة Groq المجانية مشتركة وليست غير محدودة. القيم الافتراضية تحميها، لكنها تحتاج ضبطًا بعد قياس الاستخدام الفعلي.
- محدد المعدل والحد اليومي داخل ذاكرة كل مثيل Node؛ عند تشغيل أكثر من مثيل استخدم Redis/API Gateway أو مخزنًا موزعًا.
- Supabase المجاني مناسب للتجربة، وقد يتوقف المشروع غير النشط وفق شروط الخطة الحالية.
- النتائج المنظمة قد تحمل معلومات حساسة حتى دون النص الخام؛ التصنيف والمراجعة البشرية لازمان.
- التنقيح القائم على الأنماط ليس بديلًا عن DLP/PII مؤسسي.
- تأكيد ZDR متغير إداري يضعه المالك بعد التفعيل الحقيقي، وليس تحققًا آليًا من عقد المزود.
- دقة المطابقة يجب قياسها على فرصة حية و5–10 مشاريع حقيقية قبل أي ادعاء سوقي.
