"use strict";const assert=require("node:assert/strict");const {analyzeResearch}=require("../src/lib/research-pipeline");const {createAnalysis,emptyElements}=require("../src/lib/research-schema");const provider={analyze:async()=>createAnalysis({elements:emptyElements()})};
(async()=>{const ok=await analyzeResearch({text:"نص بحثي صالح للتحليل والتقييم"},{provider});assert.equal(ok.result.analysisVersion,"rafid.research-readiness.v1");await assert.rejects(()=>analyzeResearch({text:"a",file:{}},{provider}),e=>e.code==="INPUT_INVALID");const slow={analyze:()=>new Promise(r=>setTimeout(()=>r(createAnalysis({elements:emptyElements()})),80))};const one=analyzeResearch({text:"بحث مكرر صالح للتحليل"},{provider:slow});await assert.rejects(()=>analyzeResearch({text:"بحث مكرر صالح للتحليل"},{provider:slow}),e=>e.code==="ANALYSIS_IN_PROGRESS");await one;const c=new AbortController();const cancelled=analyzeResearch({text:"نص إلغاء صالح للتحليل"},{provider:slow,signal:c.signal});setTimeout(()=>c.abort(),5);await assert.rejects(()=>cancelled,e=>e.code==="TIMEOUT");console.log("Rafid end-to-end analysis pipeline tests passed.");})().catch(e=>{console.error(e);process.exitCode=1;});
