"use strict";const assert=require("node:assert/strict");const {createAnalysis,emptyElements}=require("../src/lib/research-schema");const {createGroqAdapter,ProviderError,normalizeModelAnalysis}=require("../src/lib/research-provider");
const client={chat:{completions:{create:async()=>({choices:[{message:{content:JSON.stringify(createAnalysis({elements:emptyElements()}))}}]})}}};const a=createGroqAdapter({client,model:"from-env",timeoutMs:50});a.analyze({messages:[],requestId:"x",textSize:10}).then(x=>assert.equal(x.analysisVersion,"rafid.research-readiness.v1")).then(async()=>{const normalized=normalizeModelAnalysis({researchSummary:"ملخص",extractedElements:{problem:{status:"موجود",summary:"مشكلة",evidence:["ص1"],assessmentNote:"واضحة"}}});assert.equal(normalized.extractedElements.problem.status,"موجود");assert.equal(normalized.technicalReadiness.score>0,true);const bad=createGroqAdapter({client:{chat:{completions:{create:async()=>({choices:[{message:{content:"{}"}}]})}}},model:"m"});await assert.rejects(()=>bad.analyze({messages:[]}),e=>e instanceof ProviderError&&e.kind==="invalid_response");console.log("Rafid resilient provider tests passed.");});
